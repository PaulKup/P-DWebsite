{
  "version": 2,
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/.next",
  "installCommand": "cd client && npm install --omit=dev",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./client"
}
